---
- qnode:
    name: root
    question: "Which kind of artifacts do you want?"
    transition_values:
      - name: infrastructure
        description: "To deployment the infrastructures, like: kubernetes..."
      - name: application
        description: "To deploy your application with code, like: php, java..."

- qnode:
    name: infrastructure
    prefix: root
    # detector: github.com/Microsoft/kunlun/quiz/cmd/language # this would be supported later, the tool will be called automatically to judge the next nodes.

- qnode:
    name: application
    prefix: root

- qnode:
    name: language
    prefix: application

- qnode:
    name: php
    prefix: language